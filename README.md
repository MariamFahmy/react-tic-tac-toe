# Link: https://react-tic-tac-toe-lake-kappa.vercel.app/

Learning from the official React documentation to ensure strong understanding and foundation in React.  

- To collect data from multiple children, or to have two child components communicate with each other, declare the shared state in their parent component instead. The parent component can pass that state back down to the children via props. This keeps the child components in sync with each other and with their parent.
- Always try to avoid redundant state to reduce bugs
